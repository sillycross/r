<def f='WebKit/Source/WebCore/platform/PasteboardItemInfo.h' l='91' ll='103' type='bool WebCore::PasteboardItemInfo::canBeTreatedAsAttachmentOrFile() const'/>
<doc f='WebKit/Source/WebCore/platform/PasteboardItemInfo.h' l='87'>// The preferredPresentationStyle flag is platform API used by drag or copy sources to explicitly indicate
    // that the data being written to the item provider should be treated as an attachment; unfortunately, not
    // all clients attempt to set this flag, so we additionally take having a suggested filename as a strong
    // indicator that the item should be treated as an attachment or file.</doc>
