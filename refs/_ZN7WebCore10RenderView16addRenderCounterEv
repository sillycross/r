<def f='WebKit/Source/WebCore/rendering/RenderView.h' l='153' type='void WebCore::RenderView::addRenderCounter()'/>
<doc f='WebKit/Source/WebCore/rendering/RenderView.h' l='149'>// FIXME: This is a work around because the current implementation of counters
    // requires walking the entire tree repeatedly and most pages don&apos;t actually use either
    // feature so we shouldn&apos;t take the performance hit when not needed. Long term we should
    // rewrite the counter code.</doc>
<use f='WebKit/Source/WebCore/rendering/RenderCounter.cpp' l='367' u='c' c='_ZN7WebCore13RenderCounterC1ERNS_8DocumentERKNS_14CounterContentE'/>
